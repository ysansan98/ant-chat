name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  quality:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare bundled rg binaries
        run: pnpm -F @ant-chat/desktop rg:prepare

      - name: Rebuild better-sqlite3
        run: pnpm -F @ant-chat/desktop rebuild:node

      - name: Build workspace packages
        run: pnpm build:packages

      - name: Check (type-check + lint + test:unit)
        run: pnpm check

  test:
    runs-on: ubuntu-latest
    needs: quality
    env:
      TZ: Asia/Shanghai

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare bundled rg binaries
        run: pnpm -F @ant-chat/desktop rg:prepare

      - name: Rebuild better-sqlite3
        run: pnpm -F @ant-chat/desktop rebuild:node

      - name: Build workspace packages
        run: pnpm build:packages

      - name: UI test
        run: pnpm -F @ant-chat/desktop test:ui

      - name: Agent E2E test
        run: pnpm -F @ant-chat/desktop test:e2e:agent-runtime
